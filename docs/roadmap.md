# ArtNearby — статус фіч (roadmap)

Живий статус реалізації — оновлюється частіше за `about.md`, тому винесений в окремий файл. Контекст проєкту (ролі, user flow, модель даних) — див. [`about.md`](./about.md).

## Авторизація та профіль
| Функція | Статус |
|---|---|
| Реєстрація (фізична особа / компанія) | Готово, є баг: не перевіряється унікальність username/email → впаде з 500 при повторі (див. [`known-issues.md`](./known-issues.md)) |
| Логін / логаут | Готово |
| Профіль користувача | Готово (базово) |
| Зміна аватара й імені в профілі | Заплановано |

## Магазини та товари (для компаній)
| Функція | Статус |
|---|---|
| Додати / редагувати / видалити свій магазин (Store) | Готово |
| Додати / редагувати / видалити свій товар (Product) | Готово |
| Категорії товарів (fine art, graphic art, papeterie, art craft) | Готово |
| Прив'язка товару до магазину й виробника (Fabricator) | Готово |
| Зображення товару | Готово |

## Пошук і перегляд товарів
| Функція | Статус |
|---|---|
| Головна сторінка з випадковими товарами | Готово |
| Повний список товарів | Готово |
| Картка товару (опис, ціна, магазин) | Готово |
| Фільтр за категорією/підкатегорією | Частково |

## Геопошук найближчого магазину (головна фіча)
| Функція | Статус |
|---|---|
| Координати магазину (Location: довгота/широта) | Модель готова |
| Геолокація користувача (browser geolocation або по IP) | Не реалізовано |
| Розрахунок відстані до магазину з товаром | Не реалізовано |
| Список магазинів поруч, відсортований за відстанню | Не реалізовано |
| Побудова маршруту (карта + route) | Не реалізовано — MapTiler SDK вже підключено в шаблоні й ініціалізовано в `static/js/main.js`, але без міток і логіки |

## Інфраструктура
| Функція | Статус |
|---|---|
| Docker (`Dockerfile`) | Готово |
| Docker Compose (`docker-compose.yml`) | Базово готово — піднімає `web` (migrate + collectstatic + runserver) |
| Seed / clean / drop management-команди (`seed_data`, `clean_data`, `drop_data`) | Готово — `seed_data` наразі з ручними списками (5 європейських міст) |
| `seed_data` на Faker замість ручних списків — генерація довільної кількості даних, з можливістю окремо наповнювати лише товари/лише магазини тощо | Заплановано |
| Окремий сервіс БД у Compose (Postgres) | Заплановано |

## Інше
| Функція | Статус |
|---|---|
| "Candle mode" — фонова фіча-пасхалка для атмосфери пошуку | Готово (є в шаблоні/стилях) |
